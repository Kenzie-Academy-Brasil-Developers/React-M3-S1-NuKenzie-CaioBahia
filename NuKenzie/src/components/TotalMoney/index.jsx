import React from 'react'
import styles from "./syle.module.css"

export const TotalMoney = ({kenzielist}) => {

  return (
    <>
   
   </>
  )
}

// Soma entradas (MAP transformar objs em array com os valores (+if) & reduce para somar todos)
// Soma despesas (MAP transformar objs em array com os valores (+if)& reduce para somar todos)

// Diferença entre elas a-b